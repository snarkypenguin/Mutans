; -*- mode: scheme; -*-
'Parameters
(define <blackboard>-parameters
  (list
    ;;(list 'label '<uninitialised>)
    (list 'message-list '())
  )
)
(set! global-parameter-alist (cons (cons <blackboard> <blackboard>-parameters) global-parameter-alist))
