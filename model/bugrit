(define-class <simple-metabolism> (inherits-from <object>) (state-variables days-of-hunger hunger-limit))
(define-class <metabolism> (inherits-from <object>) (state-variables stomach-contents mass structural-prop structural-mass max-consumption-rate metabolic-rate starvation-level gut-size condition food->condition-conversion-rate condition->food-conversion-rate food->mass-conversion-rate mass->food-conversion-rate condition-conversion-rate mass-conversion-rate max-condition-rate max-growth-rate))
(define-class <simple-animal> (inherits-from <simple-metabolism> <thing>) (state-variables age sex habitat searchradius foodlist homelist breedlist domain-attraction food-attraction nominal-growth-rate population-switch))
(define-class <animal> (inherits-from <simple-animal>) (state-variables current-interest movementspeed searchspeed foragespeed wanderspeed objective near-food-attraction))
(define-class <example-animal> (inherits-from <animal>) (state-variables mass peak-mass sated-quantity-prop mort-prob age cell crowded-level reproduction-mass reproduction-prob reproduction-delay reproduction-ct prey-list eat-limit forage-ct migrate-param sated-time hunger-time hunger-moving-average seed-lag-list*))
(define-class <diffeq-system> (inherits-from <agent>) (state-variables variable-names variable-values variable-symbols d/dt-list subdivisions variable-values get-externals external-update domains variable-definitions too-small epsilon do-processing P))
(define-class <tracked-agent> (inherits-from <agent>) (state-variables track tracked-paths track-schedule track-epsilon))
(define-class <thing> (inherits-from <tracked-agent>) (state-variables mass dim location direction speed))
(define-class <environment> (inherits-from <agent>) (state-variables default-value minv maxv rep))
(define-class <blackboard> (inherits-from <agent>) (state-variables label message-list))
(define-class <model-maintenance> (inherits-from <object>) (state-variables maintenance-list I-need))
(define-class <introspection> (inherits-from <agent>) (state-variables introspection-list timestep-epsilon))
(define-class <ecoservice> (inherits-from <agent>) (state-variables running-externally external-rep-list ext-get ext-set! name sym patch value capacity r delta-T-max do-growth growth-model history))
(define-class <population-system> (inherits-from <ecoservice>) (state-variables patch value capacity r delta-T-max do-growth growth-model history))
(define-class <polygon> (inherits-from <object>) (state-variables locus perimeter radius))
(define-class <circle> (inherits-from <object>) (state-variables locus perimeter radius))
(define-class <patch> (inherits-from <environment>) (state-variables service-list))
(define-class <dynamic-patch> (inherits-from <patch> <diffeq-system>) (state-variables do-dynamics do-growth subdivisions))
(define-class <landscape> (inherits-from <environment>) (state-variables terrain-function))
(define-class <habitat> (inherits-from <landscape>) (state-variables patch-list dump-times scale internal-runqueue))
(define-class <habitat*> (inherits-from <habitat>) (state-variables global-patch global-update))
(define logger-tags '())
(define-class <log-introspection> (inherits-from <introspection>) (state-variables file filename filetype format variables variables-may-be-set missing-val show-field-name preamble-state introspection-list timestep-epsilon))
(define-class <logfile> (inherits-from <log-introspection>) (state-variables))
(define-class <snapshot> (inherits-from <log-introspection>) (state-variables lastfile currentfile))
(define-class <log-map> (inherits-from <snapshot>) (state-variables ps png))
(define-class <log-data> (inherits-from <logfile>) (state-variables))
(define-class <monitor> (inherits-from <introspection>) (state-variables selector configuration-candidates domain*-options niche*-options assessment-function history target-list specific-agents KnownGood KnownBad))
(define-class <domain-monitor> (inherits-from <monitor>) (state-variables domain-list domain-selector))
(define-class <niche-monitor> (inherits-from <monitor>) (state-variables representation-alternatives domain niche-selector))
(define-class <agent-monitor> (inherits-from <monitor>) (state-variables zones indicator-function))
(define-class <simple-plant> (inherits-from <model-maintenance> <thing>) (state-variables max-age max-mass age lai water-stress water-use water-stress-effect reproduction-mass reproduction-period reproduction-offset reproduction-mechanism fruiting-rate seeds-per-fruit habitat population-switch))
(define-class <example-plant> (inherits-from <model-maintenance> <thing>) (state-variables cell peak-mass mass fruiting-mass fruiting-prob fruiting-rate seeds-per-fruit seed-queue mort-mass mort-prob seeds-per-fruit habitat))
