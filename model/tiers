remodel
	= support-lib
	  + utils.scm
	  + units.scm
	  + sort.scm
	  + maths.scm
	  	 + utils.scm
	  + integrate.scm
	  	 + maths.scm
		 + utils.scm
	  + matrix.scm
	  	 + maths.scm
	  + postscript.scm
		 + utils.scm
	= sclos.scm
	= remodel-controls.scm
	= remodel-classes.scm
	  + log-classes.scm
	= remodel-declarations.scm
	  + log-declarations.scm
	= remodel-methods.scm
	  + log-methods.scm	
	+ kernel.scm
	  + postscript.scm
	+ model-configuration.scm
	