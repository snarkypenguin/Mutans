'Parameters
(define <diffeq-system>-parameters
  (list
    (list 'variable-names '())
    (list 'variable-values '())
    (list 'variable-symbols '())
    ;;(list 'd/dt-list '<uninitialised>)
    ;;(list 'subdivisions '<uninitialised>)
    ;;(list 'get-externals '<uninitialised>)
    ;;(list 'external-update '<uninitialised>)
    ;;(list 'domains '<uninitialised>)
    ;;(list 'variable-definitions '<uninitialised>)
    ;;(list 'too-small '<uninitialised>)
    ;;(list 'epsilon '<uninitialised>)
    ;;(list 'do-processing '<uninitialised>)
    ;;(list 'P '<uninitialised>)
  )
)
(set! global-parameter-alist (cons (cons <diffeq-system> <diffeq-system>-parameters) global-parameter-alist))
