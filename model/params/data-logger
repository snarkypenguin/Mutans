'Parameters

(define param:data-logger (/ (* 150 kg) (* 37 years)))

(define data-logger-parameters ;; Parameters for Botanicus exemplarii
  (list
  	(list 'name "data-logger")
  )
)
(set! global-parameter-alist (cons (cons "data-logger" data-logger-parameters) global-parameter-alist))
